# The code in lib/aws is meant to work with Python 3.13 and uses the
# default options for formatting (88 character lines, double quotes).

extend = "../../pyproject.toml"
target-version = "py313"

# All of these are defaults
lint.ignore = []
line-length = 88
format.quote-style = "double"
