[tool.ruff]
select = [
    "E",       # pycodestyle
    "F",       # pyflakes
    "I",       # isort
    "G",       # flake8-logging-format
]
exclude = [
    ".git/",
]
ignore = [
    "E731",  # Do not assign a `lambda` expression, use a `def`
]
line-length = 118
