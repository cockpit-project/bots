[tool.ruff]
select = [
    "E",       # pycodestyle
    "F",       # pyflakes
    "I",       # isort
]
exclude = [
    ".git/",
]
ignore = [
    "E731",  # Do not assign a `lambda` expression, use a `def`
]
line-length = 118
